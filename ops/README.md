# metaprev-ops

Per-product business operations + marketing home for **metaprev** (CLI to preview/validate OpenGraph cards locally, `@forsvn/metaprev`).

| Path | What |
|---|---|
| [`brand/`](brand/) | Brand vault — **stub** (`BRAND.md` only). No identity defined yet; defaults to FORSVN house tokens until built out. |

## Wiring

Business ops here are paired with the product code/surfaces below. Machine-readable: [`product.json`](product.json).

| Surface | Dir | Repo | URL |
|---|---|---|---|
| Private CLI source | `../app/` | `hungv47/ipse` | npm `@forsvn/metaprev` |
| Public mirror | `../app/` | `forsvn-labs/metaprev` | GitHub |

No dedicated marketing surface yet (no landing page).

Marketing artifacts (campaigns, content, ad/landing briefs, produced assets) for metaprev live here as they're created.

Shared, cross-product state lives in `~/ipse/forsvn/achievements/` (the `ipse/01-business` source of truth).
